import { StyleSheet } from "react-native";

export function customStyles() {
    const body = {
        text: {
            fontSize: 20, 
            fontWeight: "bold", 
            color: "#5682B1",
              },
    }
    return StyleSheet.create(body);
 }
